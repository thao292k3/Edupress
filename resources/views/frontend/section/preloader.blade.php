{{-- @foreach($all_slider as $item)
<div class="hero-slider-item" style="background-image: url('{{ asset($item->image) }}'); height: 500px; min-height: 500px; background-position: center;">
    <div class="container">
        <div class="hero-content">
            <div class="section-heading">
                <h2 class="section__title text-black fs-45 lh-60 pb-3">{{$item->title}}</h2>
                <p class="section__desc text-black pb-4">{{$item->short_description}}</p>
            </div>
            </div>
    </div>
</div>
@endforeach --}}
